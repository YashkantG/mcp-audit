- id: mcp-triage
  name: mcp-triage
  description: Security scanner for MCP (Model Context Protocol) servers
  entry: mcp-triage scan
  language: python
  pass_filenames: false
  always_run: true
  args: ["."]
