[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "mcp-triage"
version = "0.4.0"
description = "Security scanner for MCP (Model Context Protocol) servers"
readme = "README.md"
requires-python = ">=3.9"
license = { file = "LICENSE" }
authors = [{ name = "Yash Gautam" }]
keywords = ["mcp", "model-context-protocol", "security", "static-analysis", "llm", "ai-agents"]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Topic :: Security",
    "Intended Audience :: Developers",
]
dependencies = [
    "typer>=0.12",
    "rich>=13.0",
    "tomli>=2.0; python_version < '3.11'",
]

[project.urls]
Homepage = "https://github.com/YashkantG/mcp-triage"
Repository = "https://github.com/YashkantG/mcp-triage"
Issues = "https://github.com/YashkantG/mcp-triage/issues"

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[project.scripts]
mcp-triage = "mcp_triage.cli:app"

[tool.setuptools.packages.find]
where = ["src"]
